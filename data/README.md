# Dataset Directory

This directory is populated by:

```powershell
python .\scripts\build_dataset.py
```

Expected outputs:

- `train.jsonl`
- `valid.jsonl`
- `test.jsonl`
- `dataset_summary.json`

The dataset is derived from files copied into `samples/source/`.

